# web (frontend)

Intentionally left empty for now — the frontend is owned separately (Kamlesh).

It will consume the API's Zod contracts from `@tradinjournal/contracts` and the standard response
envelope (`{ data, meta }` / `{ error }`).
